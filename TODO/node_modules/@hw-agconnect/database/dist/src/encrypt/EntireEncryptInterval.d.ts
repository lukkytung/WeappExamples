import { SecretKeyManager } from './SecretKeyManager';
import { EncryptTaskManager } from '../sync/EncryptTaskManager';
export declare class EntireEncryptInterval {
    private intervalTimeOut;
    private secretKeyManager;
    private encryptTaskManager;
    private timeOutStateMachine;
    private firstTime;
    constructor();
    setIntervalCleanKey(operationStatus: boolean): void;
    saveSecretKeyManager(secretKeyManager: SecretKeyManager): void;
    saveEncryptTaskManager(encryptTaskManager: EncryptTaskManager): void;
}
