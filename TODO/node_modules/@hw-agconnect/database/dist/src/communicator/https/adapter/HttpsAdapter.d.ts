export declare abstract class HttpsAdapter {
    request(object: unknown): void;
    protected abstract realRequest(object: any): void;
}
export declare const enum HTTP_STATUS_CODE {
    OK = 200,
    MULTIPLE_CHOICES = 300,
    METHOD_NOT_ALLOWED = 405
}
