import { CertificateService } from '../../security/CertificateService';
import { HttpsAdapter } from './adapter/HttpsAdapter';
export declare const enum HttpMethod {
    POST = "POST",
    GET = "GET",
    DELETE = "DELETE",
    PUT = "PUT"
}
export declare class HttpsClient {
    private static httpAdapterClazz;
    private productId;
    private certificateService;
    private httpAdapter;
    constructor(certificateService: CertificateService);
    static setClient(clazz: new () => HttpsAdapter): void;
    send(path: string, method: HttpMethod, param: object, callback: (flag: boolean, reason: any) => void): Promise<void>;
    private processRequest;
    private requestForSync;
}
