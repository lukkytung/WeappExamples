import { SyncTask } from '../sync/task/SyncTask';
export declare class CommunicateTask {
    protected func: () => any;
    private delayMs;
    private isCanceled;
    private readonly handle;
    private readonly task;
    constructor(task: SyncTask, delayMs: number, handle?: any);
    execute(): void;
    getDelayMs(): number;
    cleanDelayMs(): void;
    cancel(): void;
}
