import '@hw-agconnect/instance';
import '@hw-agconnect/network';
import { WebSocketAdapter } from './WebSocketAdapter';
export declare class AGCWebSocketAdapter implements WebSocketAdapter {
    private listeners;
    private readonly agcWebsocket;
    constructor(listeners: any, region: string);
    connect(object: any): void;
    close(object?: any): void;
    sendMessage(object: any): void;
}
