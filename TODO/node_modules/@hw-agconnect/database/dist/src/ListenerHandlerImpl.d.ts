import { NaturalStore } from './base/NaturalStore';
import { ListenerHandler } from './ListenerHandler';
export declare class ListenerHandlerImpl implements ListenerHandler {
    private naturalStore;
    private listenerId;
    private callback;
    constructor(naturalStore: NaturalStore, listenerId: number, callback: Callback);
    remove(): Promise<void>;
}
export interface Callback {
    remove(handler: ListenerHandler): void;
}
