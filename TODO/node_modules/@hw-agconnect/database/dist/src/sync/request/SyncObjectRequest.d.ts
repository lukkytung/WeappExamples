import { SyncRequest } from './SyncRequest';
import { OperationType } from '../utils/MessageType';
export declare class SyncObjectRequest extends SyncRequest {
    constructor();
    private operationType_;
    private objectList_;
    get objList(): any[];
    set objList(sourceObjectList: any[]);
    get operationType(): OperationType | undefined;
    set operationType(sourceObjectList: OperationType | undefined);
}
