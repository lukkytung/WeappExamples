export declare class CallbackTask {
    private timerHandler;
    private timeoutMs;
    private isDone;
    constructor();
    private timeoutMonitor;
    private execute;
    executeWithTimeout(timeoutMs: number, handle?: () => Promise<any>): Promise<any>;
}
