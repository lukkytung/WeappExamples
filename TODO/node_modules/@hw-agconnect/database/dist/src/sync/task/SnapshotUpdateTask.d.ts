import { SyncResult } from '../request/SyncResult';
import { ErrorCode } from '../../utils/ErrorCode';
export declare class SnapshotUpdateTask {
    private readonly onPush;
    constructor(handle: (errorCode: ErrorCode, syncResult: SyncResult) => any);
    onResponse(response: any, result: SyncResult): void;
}
