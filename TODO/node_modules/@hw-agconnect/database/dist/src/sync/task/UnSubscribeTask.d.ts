import { WebSocketTask } from './WebSocketTask';
import { ErrorCode } from '../../utils/ErrorCode';
import { SyncResult } from '../request/SyncResult';
import { naturalcloudsyncv2 } from '../../generated/syncmessage';
import { SyncRequest } from '../request/SyncRequest';
import { SyncRequestProcessor } from '../SyncRequestProcessor';
import SyncResponseMessage = naturalcloudsyncv2.SyncResponseMessage;
export declare class UnSubscribeTask extends WebSocketTask {
    private onUnSubscribeResponse;
    constructor(syncRequest: SyncRequest, keyVersion: number, handle: (errorCode: ErrorCode, syncResult: SyncResult) => any, syncProcessor: SyncRequestProcessor);
    onResponse(response: SyncResponseMessage, result: SyncResult, syncProcessor: SyncRequestProcessor): void;
    request(): void;
}
