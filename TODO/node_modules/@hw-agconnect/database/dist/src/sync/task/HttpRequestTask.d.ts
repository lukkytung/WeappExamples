import { ErrorCode } from '../../utils/ErrorCode';
import { SyncResult } from '../request/SyncResult';
import { SyncRequest } from '../request/SyncRequest';
import { SyncTask } from './SyncTask';
import { CertificateService } from '../../security/CertificateService';
export declare class HttpRequestTask extends SyncTask {
    private static readonly httpResponseMap;
    private readonly certificateService;
    constructor(syncRequest: SyncRequest, keyVersion: number, certificateService: CertificateService, handle: (errorCode: ErrorCode, syncResult: SyncResult) => any);
    request(): void;
    onResponse(message: any): void;
}
